# Planes de ejecución

Aquí viven los planes que ejecuta el orquestador del flujo de dos IAs (`docs/roles/`). Cada plan es un documento Markdown con tareas numeradas y checkboxes (`- [ ]`) que el orquestador marca (`- [x]`) en el commit de cada tarea. El usuario indica qué plan está vigente al arrancar el bucle.
